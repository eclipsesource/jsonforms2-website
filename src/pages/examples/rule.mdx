---
name: Rule
route: /examples/rule
---

import Rule from '../../components/examples/Rule';

# Rule

This example uses two [rules](/docs/uischema/rules). The first rule controls the enablement of a control if the 'Is Alive' checkbox is unchecked.
The second rule controls the visability of a control if the "Eats vegetables?" checkbox is checked.
Give it a try!

<Rule />
